//importing the type object from the file and assigning it to a variable to use in the file.

import { testCase } from "./types.js";

const myTest: testCase = { name: "Login", status: "Passed", duration: 350 };
console.log(myTest);



