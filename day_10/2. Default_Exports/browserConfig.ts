//default exports are used when the function or class is the only thing that is exported from the file.
export default function launchBrowser(name: string): void {
    console.log(`Launching: ${name}`);
}